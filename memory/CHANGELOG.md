# CHANGELOG — SIRIUS

## 08/06/2026 (soir) — Sécurisation complète auth & isolation utilisateur
Contexte : iteration_35 avait signalé 5 échecs, iteration_36 en a confirmé 4 corrigés + révélé 4 nouvelles failles, iteration_37 = 34/36 puis 36/36 après correctifs finaux.

### Corrigé et vérifié (testing agent + curl)
- **Lockout login** : compteur basé uniquement sur l'email normalisé (`ident=login:<email>`), plus d'IP (l'ingress alternait 2 IPs et fragmentait le compteur). 5 échecs → 429.
- **Seed admin idempotent** : `seed_admin_and_indexes` met à jour `password_hash` si `ADMIN_PASSWORD` change.
- **local-memory** : `require_user` sur GET/POST/PUT/DELETE, filtrage par `user_id` (B ne peut plus toucher les faits de A).
- **Chat** : `/api/chat`, `/api/chat/stream`, `/api/chat/reset` exigent l'auth (401 anonyme, y compris texte vide — auth AVANT validation), rate-limit par `user_id`, `session_id` préfixé `uid:` (isolation A/B totale).
- **Agora CRM** (modules_api.py) : helper `_agora_uid` — toutes les routes (deals CRUD, objectif, relance, coach, coach/history, export.csv) exigent l'auth et filtrent par `user_id`. Deals créés avec `user_id`. Données legacy (sans user_id) revendiquées automatiquement par l'admin à sa première requête (agora_deals, agora_settings, agora_coach_history).
- **Payments** (payments_api.py) : helper `_uid` — toutes les routes (deal-checkout, status, deals-status, transactions, receipt, remind) exigent l'auth, transactions stockées/filtrées par `user_id`, checkout vérifie la propriété du deal. `/api/stripe/webhook` reste public (signature Stripe). Fallback `sk_test_emergent` codé en dur supprimé (clé uniquement via env).
- **CORS** : `allow_origin_regex` limité aux domaines `emergentagent.com`/`emergent.host` + `allow_credentials=true`. NOTE : l'ingress Cloudflare réécrit ACAO=* sur l'URL publique (hors contrôle, sans impact : frontend/backend same-origin). Backend direct vérifié correct.
- **Corruption préexistante** : `client.close()se()` en fin de server.py réparé.

### Régression validée
- Stripe test-mode (URL cs_test réelle), reçu PDF ReportLab (%PDF), rappel email SMTP : OK (iteration_36).
- Frontend : login admin + boot HUD vérifiés par capture. Frontend 100% vert en iteration_35.

### Leçon (récurrente)
- Les éditions `search_replace` peuvent se perdre silencieusement (hot reload / checkpoint) : TOUJOURS re-grep après édition (2 éditions perdues cette session : import require_user ligne 22 server.py, user_id dans deal modules_api.py).

### Rapports
- /app/test_reports/iteration_36.json (16/20), /app/test_reports/iteration_37.json (34/36 → correctifs edge cases appliqués et auto-vérifiés curl).

## 08/06/2026 (nuit) — Isolation Thémis par utilisateur + panneau Admin
- **Thémis isolé** (themis_api.py) : helper `_uid` (require_user + revendication legacy admin sur les 6 collections themis_*). Toutes les routes (clients, items, docs, from-deal, orders, payments, pieces, export, bilan, stats) exigent l'auth et filtrent par `user_id`. `/themis/templates` reste public. Bilan tutoie désormais.
- **from-deal & relance** : deal/client recherchés avec `user_id` (plus de fuite cross-compte par nom de client).
- **Admin** : `GET /api/admin/users` (403 non-admin) → comptes + activité (messages, dernière activité, souvenirs, deals, transactions, docs/clients Thémis), sans password_hash. `_public()` expose `role`.
- **Frontend** : badge ADMIN dans le chip (AuthGate), `AdminPanel.jsx` (table des comptes), entrée menu « ADMINISTRATION » visible admin uniquement (App.js), styles App.css.
- **Testé** : iteration_38 (backend 39/47 → les 8 échecs venaient de 4 routes pieces/export dont l'édition avait été PERDUE ; réappliquée et re-vérifiée par curl A/B : 401 anonyme, 404 inter-comptes, export ZIP isolé). Frontend admin/non-admin 100 % vert.
- ⚠️ Leçon récurrente confirmée ×3 : re-grep OBLIGATOIRE après chaque search_replace (éditions silencieusement perdues).

## 08/06/2026 — Lecture à voix haute universelle
- Nouveau module générique `/app/frontend/src/readAloud.js` (initReadAloud dans App.js, même pattern que uiSounds/holoFx) : MutationObserver global sur .prime-screen/.zeus-screen/.hud-panel/.central-card/.holo-popup.
- Dès qu'un contenu textuel ≥ 160 caractères apparaît/est généré dans N'IMPORTE QUEL module, barre dorée « Veux-tu que je te lise ça à voix haute ? » (data-testid read-aloud-bar) en bas de la fenêtre. OUI = lecture speakFr du contenu À JOUR (cap 2500 car.), bouton devient ARRÊTER. NON = silencieux tant que la fenêtre reste ouverte.
- Anti-spam : re-proposition seulement si la longueur du texte varie de > 80 caractères (les jauges Zeus qui se rafraîchissent toutes les 8 s ne re-déclenchent pas). Débounce 1,2 s (compatible effet machine à écrire des popups). Boutons/inputs/éléments [data-no-read] exclus de l'extraction. Désactivable via localStorage sirius_read_aloud=off.
- CSS : .read-aloud-bar absolute bottom centrée, offset spécial .zeus-screen (78px au-dessus du champ), static dans .holo-popup.
- Testé par 4 captures : apparition (Zeus, Admin), position basse, OUI→ARRÊTER, NON définitif malgré refresh 10 s.

## 08/06/2026 — Réponse vocale oui/non + réglage lecture + gestion des comptes admin
- **Réponse vocale** : `window.__siriusReadAloudAnswer(texte)` exposé par readAloud.js — consommé en priorité par le micro principal (hook dans handleTranscript, App.js ~3217) ; sinon mini-écoute one-shot 7 s (fr-FR) démarrée à l'apparition de la barre UNIQUEMENT si micro principal inactif (window.__siriusMicOn, 3 points de bascule dans App.js) ET permission micro déjà accordée ET Sirius ne parle pas. Regex oui/ouais/ok/d'accord/vas-y/lis vs non/nan/pas maintenant/annule/stop. Indice visuel « — dis "oui" ou "non" » dans la barre.
- **Réglage** : interrupteur ACTIVÉE/COUPÉE dans Configuration → onglet HUD (localStorage sirius_read_aloud, data-testid setup-readaloud-on/off), vérifié en capture.
- **Admin comptes** : PUT /api/admin/users/{uid}/disable (bloque login 403 + require_user 403, données conservées) et DELETE /api/admin/users/{uid} (cascade : 14 collections Mongo + sirius_chats par préfixe + google_calendar + SQLite via delete_user_data() de local_memory.py + fichiers pièces Thémis sur disque). Protections : pas d'auto-désactivation/suppression, pas de cible admin. UI AdminPanel : boutons Ban/CheckCircle2 + ConfirmButton corbeille (double-clic), badge DÉSACTIVÉ, ligne grisée, note explicative.
- **Testé** : curl (désactivation→login 403→réactivation→200, suppression cascade vérifiée en base, auto-suppression 400) + captures UI (boutons, badge toggle, réglage). Réponse vocale non testable en headless (pas de micro) — hooks vérifiés par grep.
- ⚠️ Édition PERDUE ×4 cette session (check disabled du login) — re-grep systématique confirmé indispensable.

## 09/06/2026 — Noyau électrique + vouvoiement présentation + image affinée
- **Noyau bleu du médaillon** : MedallionRing reçoit `status` — couches .medallion-core-glow (disque radial + 2 anneaux d'arcs électriques repeating-conic-gradient masqués en anneau) avec animation RGB hue-rotate + pulsation + flicker. Actif uniquement en thinking (frénétique)/speaking (ample)/listening (doux), invisible au repos. Vérifié par capture : classe core-thinking + arcs visibles pendant réflexion.
- **Page de présentation (boot)** : voix « à ta disposition » → « à votre disposition » (seule occurrence de tutoiement sur cette page). Le reste de l'app reste au tutoiement.
- **Image de fond antique.jpg** : UnsharpMask (radius 2, 110 %) + luminosité 0.86 + contraste 1.08 via PIL. Backup /app/memory/antique_v4_backup.jpg. Cache service worker bumpé sirius-v4 → sirius-v5.

## 09/06/2026 — Nouveau module PORTUS NUMMARIUS# (bourse & marchés)
- **Backend** `/app/backend/nummarius_api.py` (routes auth require_user) : GET /api/nummarius/market (7 actifs : AAPL/MSFT/NVDA/TSLA + BTC/ETH/SOL avec prix, variation, sparkline 30 pts) et GET /api/nummarius/history/{id}?range=1s|1m|3m|max. Sources : Alpha Vantage (cache 6 h, quota 25/j) avec REPLI AUTOMATIQUE Yahoo Finance (query1.finance.yahoo.com, sans quota, User-Agent requis) quand AV épuisé ; cryptos CoinGecko market_chart (cache 15 min). Erreurs en 429 (JAMAIS 502/503 → l'ingress les remplace par une page HTML).
- **Frontend** `/app/frontend/src/PortusNummarius.jsx` : personnage généré (gemini image → /holo/nummarius.jpg « GARDIEN DES MARCHÉS »), liste d'actifs avec sparklines SVG, graphique recharts AreaChart (hauteur FIXE 340 — height="100%" ne se rend pas), boutons de période. CSS .numm-* (attention : .gcal-body impose max-width 720px + column → .numm-body les surcharge en row / 1180px).
- **Intégration** : entrée menu PANTHÉON, entrée « PORTUS NUMMARIUS — Bourse & marchés » sur la page de présentation (boot) cliquable (openers + boot list), state showNummarius.
- **Testé** : curl (7 actifs live, historiques AAPL 66 pts + BTC 121 pts, 401 sans cookie) + captures (boot entry, panneau, avatar, courbe, changement actif/période).

## 09/06/2026 — Fenêtres holographiques 3D partout (drag/resize/close)
- **holoWindows.js** (initHoloWindows dans App.js) : MutationObserver global.
  - Grands modules (.prime-screen/.zeus-screen/.eu-screen/.setup-screen/.iw-panel) → .holo-win : fixed flottant (86 % viewport, cascade 36 px), drag par en-tête (.zeus-head/.eu-head/.gcal-head sinon barre injectée), poignée resize sticky bas-droit, z-index 1001-1190 (SOUS les contrôles HUD 1200/1500 ; menu modules monté à 9600), géométries mémorisées localStorage sirius_holo_geom, clic → premier plan, neutralisation du « clic à côté ferme » des setup-screen (stopPropagation capture si target===panel).
  - Popups (.central-card/.holo-popup) → decoratePopup : barre drag + poignée resize injectées ; freeEl calcule l'OFFSET DU CONTAINING BLOCK TRANSFORMÉ (fixedOffset — position:fixed est relatif à l'ancêtre transformé, ex .reactor-wrap) sinon double décalage ; clic-fermeture supprimé après vrai drag (click capture once).
- **CSS** : .holo-win verre cyan translucide + grille/scanlines EN BACKGROUND MULTI-COUCHES (les pseudo-éléments fixed/absolute se positionnent mal dans un scroller transformé), flottement holoWinFloat (perspective rotateX 1.3-1.9° + translateY), en-têtes sticky, resize sticky !important (une règle globale force position:relative). PAS de `inset:auto !important` (écrase left/top inline !). Bornes resize : w ≤ innerWidth-x, h ≤ innerHeight-y. Style holo aussi sur .web-window/.sirius-display/.sirius-progress (drag/resize natifs conservés) et .central-card.
- readAloud MIN_CHARS 160→110.
- **Testé** : testing_agent iteration_39 (9 types de fenêtres verts, 4 défauts) puis les 4 corrigés et re-vérifiés par captures : drag carte dx exact 180, resize carte 212→364, fermeture clic, 3 modules ouverts via menu simultanément, menu au-dessus des fenêtres.
- PIÈGE headless : l'InstallWizard s'ouvre auto 12 s après login si localStorage vierge → poser sirius_installed AVANT chargement (add_init_script).

## 09/06/2026 — Réduction des fenêtres en pastilles
- holoWindows.js : bouton .holo-win-min (—) injecté dans chaque barre de fenêtre holo ; minimize() masque la fenêtre (.holo-minimized display:none) et crée une pastille .holo-dock-pill dans #holo-dock (fixed bottom 64px centré, z 1195, au-dessus des fenêtres 1190 / sous les contrôles 1200). Clic pastille = restaure + premier plan. Titre extrait de .zeus-title/.eu-title/.gcal-title sinon data-testid. Si React démonte une fenêtre réduite, la pastille est nettoyée (removedNodes de l'observer + garde contains au clic).
- Testé par capture : réduire Admin → pastille dorée/cyan au dock, restaurer, fermer. (Nummarius affiche proprement l'avertissement CoinGecko rate-limit dans .numm-warn.)

## 09/06/2026 — Commande « range tout »
- holoWindows.js : export minimizeAll() (réduit toutes les .holo-win visibles, retourne le compte).
- App.js processCommand (~ligne 3157, avant la section musique) : regex (range|réduis|minimise|ramasse) + (tout|les fenêtres) → minimizeAll + confirmation vocale (« C'est rangé, N fenêtres réduites en pastilles »). Fonctionne au micro ET au clavier (même pipeline).
- Testé par capture : 2 fenêtres → 2 pastilles + message parlé.

## 09/06/2026 — Ouverture vocale des modules
- App.js : openModuleByName (useCallback avant processCommand, ~ligne 2477) + moduleItemsRef synchronisé après la déclaration de moduleItems. Regex dans processCommand (avant « range tout ») : (ouvre|affiche|restaure|rouvre) + nom. Priorité : 1) pastille du dock correspondante → restauration ; 2) moduleItems par label/id/ALIASES (bourse→nummarius, zeus→cortex, facturation→themis, agenda→gcal, comptes→admin…). Confirmation vocale (« J'ouvre PORTUS NUMMARIUS# »). Non-correspondance → la commande continue son traitement normal (pas de conflit avec « montre le système », musique, archives).
- Testé par capture : « ouvre la bourse » → Nummarius ; « range tout » puis « ouvre la bourse » → restauration pastille ; « affiche thémis » ; régression carte système OK.

## Juin 2026 — Session « NLP vocal, Voyant, Web Agent »
- **Compréhension vocale naturelle (Groq)** : endpoint /api/intent (parse_intent + INTENT_PROMPT dans sirius_brain.py) → JSON {action, target, say}. Frontend resolveIntent/executeIntent dans App.js (fallback conversation si action=none). Actions : open/close/minimize module, minimize_all, stop_reading, stop/play_music, vision_look, web_agent. Fallback modèle llama-3.1-8b-instant, timeout frontend 13s.
- **Titre ΣIRIUS or 3D** : nouveau /holo/sirius-title.png (généré depuis la référence utilisateur, détouré noir→alpha, lettres opaques). Affiché : login, page de présentation (boot-title-img, animation conservée), HUD central (sirius-hud-title superposé au titre peint via MedallionRing, left 52.3% top 38.9%).
- **SIRIUS Voyant (vision caméra)** : /api/vision/analyze (auth + validation base64) → vision_look() Kimi K3 multimodal → {description, texte_extrait, synthese}. Déclenchement vocal « regarde ça / que vois-tu » = auto-capture (VisionModule autoAnalyze via analyzeRef). Affiche vignette + description + OCR, parle la synthèse courte.
- **Voix Neural2-F féminine premium** : préréglage 1 clic dans Configuration → VOIX (setup-voice-preset-neural2f) : fr-FR-Neural2-F, débit 1.00×, gravité +2. Slider gravité étendu à +4. Fallback navigateur féminin (FEMALE regex voice.js).
- **SIRIUS Web Agent (Playwright)** : /api/webagent/run — SerpAPI (résultats Google) + Chromium headless (PLAYWRIGHT_BROWSERS_PATH=/pw-browsers dans backend/.env) : navigue vers les 3 premiers résultats (saute 403/captcha/blocage), ferme les bannières cookies, capture JPEG sauvée en Médiathèque (dossier Photos), page résultats stylée SIRIUS en secours. Frontend launchWebAgent (fenêtre tâche + capture + résumé vocal), regex « cherche X sur le web » + intention Groq web_agent. Anti-SSRF (_url_publique).
- **Sécurité Médiathèque (CRITIQUE corrigé)** : toutes les routes /api/files* et /api/archive* exigent require_user + isolation user_id (_file_scope, anciens fichiers sans user_id restent visibles).
- **UI** : pastille utilisateur (auth-chip) déplacée sous la barre (top:46px right:14px) — fin du chevauchement avec RECHERCHER signalé par l'utilisateur. Fenêtres holo : animation flottement en pause au survol + prefers-reduced-motion.
- Tests : iteration_40 (16/16 backend intent), iteration_41 (vision/titre — bugs corrigés), iteration_42 (Web Agent + voix 100 % des features, sécurité fichiers corrigée ensuite + vérifiée curl).
- Backlog restant : Résumé Quotidien (P1), Alertes De Cours (P2), CORS credentialed cross-origin (infra ingress, mineur).
- **Résumé Quotidien enrichi** : le briefing matinal (1ère connexion du jour) inclut désormais — agenda du jour (GET /calendar/events, filtré sur aujourd'hui, ignoré si Google non connecté), rappels d'échéances Thémis (retards + échéances sous 7 jours via /themis/bilan), tendances Portus Nummarius (plus forte hausse/baisse via /nummarius/market). 3 fetchs en Promise.all dans l'effet briefing d'App.js (~ligne 3838). Testé e2e : briefing délivré avec « Tendances Portus Nummarius : TESLA en tête à plus 2.8 pour cent... ».
- **Briefing à la demande** : « refais-moi le briefing / résumé du jour / donne-moi mon briefing » (regex processCommand ~ligne 3210) + intention Groq daily_briefing (formulations libres : « c'est quoi le programme aujourd'hui »). Refactor : logique extraite dans runBriefing(force) (useCallback, App.js ~3817) appelée par l'effet 1ère-connexion-du-jour ET via runBriefingRef par processCommand/executeIntent. Tâche progress silencieuse (le handler annonce déjà). Testé e2e : ack immédiat + briefing complet délivré en ~42 s.
- **Corrections titre HUD (3 demandes utilisateur)** : 1) grand Σ doré au-dessus du nom EFFACÉ du fond antique.jpg (édition Nano Banana sur zone recadrée + recalage affine ORB 282/300 inliers + greffe feutrée ; sauvegarde antique_avant_nettoyage.jpg) ; 2) tache noire entre I et R supprimée = contre-forme du R rendue transparente dans sirius-title.png (détourage v3 : coeur = pixels dorés seulement, ombres portées semi-transparentes, zones sombres opaques >400px converties en alpha lumineux) ; 3) titre recentré (left 52.3% → 50% du médaillon, le titre peint décalé n'existant plus).
- **Position finale du titre HUD validée par l'utilisateur** : .live-medallion-title → left 50%, top 37.5%, width 42% (au-dessus de la tête du gardien, centré dans le noyau).
- **Optimisation performances (validée iteration_43, 89% régression OK)** : lazy loading de ~39 modules via lazyModules.js (React.lazy + Suspense) ; horloge/CPU/RAM/volume-parole isolés dans liveStats.js (LiveTime/LiveDate/CpuRamMini/useLiveStats + statusPulseRef.volume) — le HUD ne se re-rend plus chaque seconde ni à 16fps pendant la parole ; animation du titre : filtre statique + pulsation d'opacité (titleBreath) au lieu d'animer drop-shadow. ATTENTION: certaines éditions search_replace ont été perdues silencieusement 2× (CentralCard, liste diagnostic) — toujours re-vérifier par grep après édition d'App.js/server.py.
- **Diagnostic Héphaïstos corrigé (3 fausses pannes)** : ORACLE → sonde light=1 (13 ms, plus de ReadTimeout 30 s) ; MONGODB → ping DB réel au lieu du processus local mongod (compatible Mongo managé en production) ; FRONTEND (node) → processus OU port 3000 OU « servi en statique (production) ». Diagnostic désormais 100 % OK en préversion. L'utilisateur doit REDÉPLOYER pour corriger l'alerte en production.
- **Fenêtres holographiques allégées (demande utilisateur)** : suppression du backdrop-filter blur(16px) (+ règle globale .holo-win * → none, coût GPU majeur avec N fenêtres), fond plus opaque (0.90/0.94) pour conserver l'aspect holographique (bordures cyan, scanlines, lueur, entrée 3D) ; animation flottante infinie supprimée (transform statique rotateX 1.2°), zeusIn/holoOut sans blur (brightness seul) ; box-shadows 5→3 ; grilles 46px retirées ; contain: layout paint ; en-têtes sticky sans blur. Canvas bridés à 30 fps (onde vocale App.js, HoloScene, SanctuaryAmbience — ReactorCore l'était déjà). Titre bourse : « # » parasite retiré.
- **Clé Google Maps (Atlas) câblée** : MAPS_PLATFORM_API_Key (backend/.env, alias GOOGLE_MAPS_API_KEY accepté) désormais exposée via /api/chat/status (gmaps_env) et injectée automatiquement dans keys.gmaps côté frontend (plus de saisie manuelle). Ajoutée au tableau /system/keys_status. DIAGNOSTIC : la clé est VALIDE mais Google renvoie ApiNotActivatedMapError → l'utilisateur doit ACTIVER « Maps JavaScript API » (+ Geocoding/Directions si itinéraires) dans console.cloud.google.com pour son projet ; la clé a des restrictions referer (normal pour clé navigateur). Action utilisateur requise, non corrigeable côté code.
- **09/06/2026 — Atlas : message d'erreur élégant** : overlay "CARTE INDISPONIBLE" (AtlasPanel.jsx + Atlas.css .atlas-map-fail) affiché sur la zone carte quand Google refuse la clé (gm_authFailure) — remplace l'écran noir, liste les 3 actions Google Cloud (activer Maps JavaScript API, restrictions referer, facturation) + bouton MODIFIER LA CLÉ. Vérifié par capture.
- **09/06/2026 — Fenêtre CONFIGURATION allégée et ajustée à l'écran** (l'utilisateur devait zoomer) : holoWindows.js decorate() → taille dédiée setup-screen (max 780x700, 92/94% viewport, reset des géométries sauvegardées trop grandes) ; App.css → .setup-screen.holo-win sans inclinaison 3D ni grille de fond, carte intérieure sans double cadre (fond/bordure/ombre supprimés, pleine largeur, la fenêtre holo scrolle) ; compactage global setup (titre 18px, marges/paddings réduits, submit 11px). Vérifié par captures (onglets PROFIL + VOIX tiennent dans 1920x800 sans zoom).
- **09/06/2026 — Mode compact global des fenêtres holographiques** : .holo-win sans inclinaison 3D (transform none, texte plus net), ombres réduites (2 au lieu de 3 + inset), scrollbar cyan fine sur toutes les fenêtres ; .holo-win .prime-card sans animation panelFloat ni backdrop-filter ; .eu-screen.holo-win padding réduit (14/20px) ; .prime-sub marge 62px→20px en fenêtre ; .iw-overlay sans blur(10px). Vérifié par capture (ZEUS CORTEX tient entièrement dans 1920x800, net et léger).
- **09/06/2026 — Tailles/positions mémorisées par module** : holoWindows.js — la géométrie (x,y,w,h) de chaque fenêtre holographique était déjà sauvegardée par clé data-testid (localStorage sirius_holo_geom) au drag/resize ; ajouts : (1) la fenêtre de réponse centrale (.central-card, clé pop:central-card) sauvegarde et restaure désormais sa géométrie via popTrack(k)/decoratePopup (bornée à l'écran) ; (2) migration unique sirius_holo_v2 qui oublie l'ancienne géométrie trop grande de sirius-setup (remplace le reset systématique qui aurait écrasé la taille choisie par l'utilisateur à chaque ouverture). Vérifié E2E Playwright : drag ZEUS CORTEX → reload → réouverture à la position exacte (PERSISTE: True).
- **09/06/2026 — « Pas de clé Kimi K3 valide » (signalé en PRODUCTION)** : sirius_brain.py — (1) _k3_answer fait un repli automatique sur la clé serveur (ENV_K3_KEY) si la clé personnelle est refusée 401 ; (2) _k3_error_msg() : messages précis selon l'échec (clé refusée / quota RateLimit / timeout-connexion « ce n'est pas un problème de clé » / erreur inattendue) au lieu du NO_KEY_MSG unique qui accusait la clé à tort (les logs montraient des APITimeoutError affichés comme « clé invalide »). Appliqué à generate_answer et generate_answer_stream. Testé curl préversion : clé invalide envoyée → repli serveur → réponse correcte + WARNING loggé. ⚠️ L'utilisateur doit REDÉPLOYER pour que la production en profite.
- **09/06/2026 — Témoin Cerveau (source de clé en direct)** : backend sirius_brain.py K3_SOURCE/k3_source() (personnelle / serveur / repli-serveur, mode rapide → serveur) posé dans _prepare_brain et le repli 401 ; server.py ajoute key_source aux réponses /api/chat et à l'événement done de /api/chat/stream. Frontend App.js : état keySource mis à jour dans applyExtras + badge d'en-tête (data-testid key-source-badge, pastille or=CLÉ PERSO / cyan=CLÉ SERVEUR / orange=CLÉ SERVEUR·REPLI, valeur initiale déduite de keys.groq/envGroq). CSS .ind-dot.gold. Testé curl (serveur + repli-serveur) et capture (badge visible, mis à jour après réponse).
- **⚠️ INCIDENT App.js (récurrence)** : pendant l'édition, la fin du fichier a été dupliquée (2× bloc boot + Root + export) → SyntaxError « Adjacent JSX elements » ligne 5148, et l'édition du badge a été perdue silencieusement. Réparé par troncature au premier « export default Root; » puis ré-application du badge. TOUJOURS re-grep App.js après édition (grep -c "export default Root" doit rendre 1).
- **09/06/2026 — Mode JARVIS (Fast Reasoning), nouveau mode IA par défaut** : sirius_brain.py — _k3_pipeline() (préparation+analyse K3 en tâche parallèle), REVISE_PROMPT + _groq_revise() (compare réponse instantanée vs analyse profonde, renvoie OK si rien à corriger, sinon version corrigée ≤3 phrases). generate_answer_stream ia_mode="jarvis" : flux Groq immédiat (1er delta ~0,5 s) → attente K3 (timeout 30 s) → événement SSE {type:"revision"} + done avec la réponse finale ; si Groq muet → formulation classique depuis l'analyse ; si K3 échoue et Groq a répondu → la réponse instantanée reste. generate_answer (non-flux) : Groq et K3 en parallèle via asyncio. Frontend : gestion événement revision (log de progression, le done met à jour le texte silencieusement), ia_mode par défaut "jarvis" (App.js + SiriusSetup), 3e bouton JARVIS (icône Rocket, recommandé) dans Configuration → IA, réinitialisation IA → jarvis. Testé curl flux : delta 0,56 s / revision+done 7,9 s avec réponse enrichie ; capture onglet IA OK (JARVIS actif).
- **⚠️ INCIDENTS ÉDITION (récurrence x2 cette session)** : queue orpheline ajoutée en fin de SiriusSetup.jsx (SyntaxError 473) + import Rocket perdu silencieusement — mêmes symptômes qu'App.js plus tôt. Réparé par troncature + ré-application. TOUJOURS vérifier la fin de fichier (tail) et re-grep les identifiants après édition des gros fichiers frontend.
- **09/06/2026 — Témoin de mode dans la barre d'activité + nettoyage HUD (demande utilisateur)** : SiriusProgress.jsx accepte prop mode (App.js passe status) et affiche dans l'en-tête une pastille « Mode : Veille/Écoute/Réflexion/Réponse » avec petite barre colorée (cyan/bleu/ambre/turquoise, CSS var --mode-c, .sp-mode/.sp-mode-bar) ; la fenêtre d'activité est désormais TOUJOURS visible (barre compacte .sp-compact sans tâches, bouton fermer/compteur seulement si tâches), largeur 360→400px. SUPPRIMÉ : les 4 boutons d'état sous la barre de chat (.demo-controls, triggerState désormais inutilisé pour l'UI) ; les animations du noyau du gardien (rotation ringSpin/ringSpinRev des 2 anneaux → statiques, bloc medallion-core-glow (disc+arcs+flicker+classes core-*) retiré du JSX et du CSS). Vérifié Playwright : indicateur visible « Mode : Veille », demo-controls=0, animationName=none, core-glow=0.
- **09/06/2026 (suite) — Cercles du noyau ré-animés (demande utilisateur « garde juste les cercles du noyau animés »)** : rotations ringSpin 28s / ringSpinRev 18s restaurées sur .live-medallion-img ; le halo central animé (core-glow disc/arcs/flicker) reste SUPPRIMÉ. Vérifié Playwright : animationName = ringSpin/ringSpinRev, core-glow = 0.
- **09/06/2026 — Widget météo « NaN° » corrigé** : cause racine = open-meteo renvoie {"reason":"Daily API request limit exceeded"} (limite quotidienne de l'IP partagée du pod) → d.current absent → Math.round(undefined)=NaN. App.js fetchWeather : garde Number.isFinite(temperature_2m) (sinon placeholder « Localisation... », jamais NaN) + 2 relances auto à 20 s + garde au rendu FloatingPanels. Vérifié Playwright : widget affiche « Localisation... », aucun NaN.
- **10/06/2026 — Veille intelligente** : App.js effet global (pointerdown+keydown) → si status idle, passe en "listening" (Écoute) dès la moindre interaction ; minuterie 25 s d'inactivité → retour "idle" (Veille) uniquement depuis listening (jamais pendant réflexion/réponse, ni micro ouvert micOnRef). La voix passait déjà en Écoute via le micro. Vérifié Playwright E2E : Veille → clic → Écoute → frappe → Écoute → 27 s → Veille.
- **10/06/2026 — Intégration Microsoft Entra ID + Microsoft Graph (les deux usages demandés)** : nouveau /app/backend/microsoft_graph.py — OAuth2 code+PKCE via login.microsoftonline.com/common (comptes perso), scopes openid profile email offline_access User.Read Mail.Read Calendars.Read. Routes : GET /api/auth/microsoft/login (302, state+PKCE stockés dans db.oauth_states), GET /api/auth/callback/microsoft-entra-id (échange code, /me Graph, liaison par microsoft_id puis email sinon création provider "microsoft", cookies JWT existants via _set_cookies, redirection /?ms=connected), GET /api/microsoft/status|/mail|/calendar/today (require_user), POST /api/microsoft/disconnect. Jetons chiffrés Fernet (MS_TOKEN_KEY) dans db.microsoft_oauth, refresh automatique (invalid_grant → déconnexion). Briefing Oracle (/api/oracle/overview, désormais avec Request) enrichi : « Agenda Microsoft : N événements » + « Outlook : N mails non lus » + payload LLM (agenda_microsoft, mails_outlook_non_lus) + ms_events/ms_unread dans la réponse. Frontend : bouton « CONTINUER AVEC MICROSOFT » (AuthGate, data-testid auth-microsoft-btn), provider Microsoft affiché dans le profil. .env : MICROSOFT_CLIENT_ID/TENANT_ID/CLIENT_SECRET/REDIRECT_URI (production) + MS_TOKEN_KEY. Testé (préversion) : 302 vers authorize avec bons paramètres, callback state invalide → /?ms=state, /microsoft/status 401 sans auth et {connected:false} connecté, oracle 200, bouton visible. FLUX OAUTH COMPLET NON TESTÉ (redirect URI enregistrée uniquement pour la production — choix utilisateur « ne teste pas »).
- **10/06/2026 — Mode démonstration SUPPRIMÉ** : App.js processCommand n'a plus le garde-fou keys.groq||envGroqRef — toute commande non-UI part vers resolveIntent (Groq/K3, clé serveur en repli automatique). Les replis localAnswer des chemins cloud (réponse vide flux/classique + cerveau injoignable) remplacés par CLOUD_RETRY_MSG (message honnête, pas de réponse démo). Badge « MODE DÉMO » → « IA CLOUD · KIMI K3 », noyau « Mode démo » → « IA Cloud active ». SiriusSetup : notes « mode démonstration » remplacées (clé personnelle optionnelle, clé serveur par défaut). CONSERVÉ : réponses locales instantanées heure/date exactes (données système, pas de LLM). Vérifié Playwright : sans clé perso (localStorage purgé), question libre → pipeline Groq/K3 complet, vraie réponse, aucun texte démo.
- **10/06/2026 — « Ouvre Outlook » ouvrait Google Calendar — corrigé à 3 niveaux** : cause = INTENT_PROMPT (Groq) sans module outlook, « gcal: agenda, calendrier » absorbait Outlook. (1) sirius_brain.py : modules outlook (boîte mail) + outlook_agenda ajoutés, gcal restreint « Google Calendar UNIQUEMENT — jamais Outlook », règle explicite anti-confusion (gcal seulement si « Google » mentionné). (2) App.js executeIntent open_module : cibles outlook→launchOutlookMail / outlook_agenda→launchOutlookAgenda. (3) Regex directes dans processCommand avant l'IA : « ouvre/lance … outlook » → boîte mail, « agenda/calendrier outlook » → agenda. Testé curl /api/intent (« ouvre outlook »→outlook, « ouvre google calendar »→gcal, « ouvre l'agenda outlook »→outlook_agenda, « ouvre mon calendrier google »→gcal) + Playwright : « ouvre outlook » ouvre OUTLOOK — BOÎTE DE RÉCEPTION, pas Google Calendar.
- **10/06/2026 — Refonte visuelle « présentation » + thème NOIR & OR (3 demandes utilisateur)** : (1) Fond du HUD : antique.jpg (gardien religieux sur trône) remplacé par le décor de la page de présentation — classe .presentation-bg (radial #05131d→#02060c + grille cyan drift + scanlines), App.js div antique-bg sans image ; anneaux d'or + titre conservés. (2) Toutes les fenêtres : bloc « THÈME NOIR & OR » en fin d'App.css — .holo-win/.setup-screen.holo-win fond noir (18,13,4→5,4,1) !important, bordures/ombres or, barre de drag, boutons min/close, poignée resize, scrollbar et titres (.zeus-title/.eu-title/h1-h3) en or ; idem .holo-pop/.central-card/.float-card + appends SiriusProgress.css et Atlas.css. (3) Chat : .cmd-bar fond noir bordure or arrondie 10px, prompt/inputs/boutons or ; subtitle-box (zone de réponse au-dessus du chat) SUPPRIMÉE du JSX — les réponses n'apparaissent plus que dans SIRIUS DISPLAY (showOnDisplay déjà en place). Vérifié Playwright : subtitle-box=0, fenêtre ZEUS CORTEX noir/or, HUD = ambiance présentation (captures).
- **10/06/2026 — Noyau présentation compact + panneaux arrondis NOIR & OR** : (1) suppression des règles CSS qui masquaient le cœur central du HUD (.center-stage .core-rings/.reactor-canvas/.reactor-text display:none + reactor-wrap::before/after none, héritées de la scène du trône) → le noyau EXACT de la page de présentation (anneaux or motifs grecs contra-rotatifs + core-pulse + ReactorCore canvas cyan + titre ΣIRIUS) est de nouveau rendu, compact (min(34vh,320px), top 30%) ; <MedallionRing> géant (840px) retiré du rendu. (2) .hud-panel (HEURE, CPU/RAM, NOYAU) et .float-card (MÉTÉO) : clip-path supprimé → border-radius 12px, fond noir dégradé, bordure or, ::after balayage or, titres/valeurs or (hud-time, fc-big, mini-bar...). Vérifié capture : HUD = présentation (noyau or/cyan compact), 4 panneaux arrondis noir/or. Note : le panneau NOYAU (bas-droite) est recouvert par la barre d'activité permanente — les deux sont déplaçables.
- **10/06/2026 — Gardien Zeus de la présentation intégré au HUD + lettres en orbite** : création /holo/zeus-gardien.png (550x1063 RGBA) — pipeline : rembg u2net_human_seg sur zeus-boot.jpg original (fidélité pixel du personnage) + masque chromatique pour l'éclair (r>170,g>130,r>b+30, bande x120-360, closing/dilation/gaussian) + conservation de la seule composante connexe principale + gomme rectangulaire des cercles dorés résiduels. (La piste « édition Gemini » avait renvoyé un JPEG à damier incrusté — inutilisable directement ; rembg installé : rembg+onnxruntime dans requirements.) App.js : img gardien-solide.png → zeus-gardien.png ; App.css : .guardian-rig réaffiché (seule .antique-jar reste masquée), position gauche 8%/top 11%/74vh conservée. Lettres grecques Σ Δ Ω Θ Φ : déjà dans le markup core-orbit — visibles et en rotation depuis la réactivation du noyau (vérifié bbox Playwright, 5 lettres). Capture : HUD = présentation complète (Zeus à gauche + noyau or/cyan + lettres).
- **10/06/2026 — Gardien réactif** : span .guardian-bolt-glow ajouté après l'img dans .guardian-rig (App.js) — halo doré (2 gradients radiaux sur la bande de l'éclair, blur 6px, mix-blend screen) invisible par défaut ; sélecteurs frère .antique-guardian.speaking/.listening + .guardian-bolt-glow → animation boltFlash (0.9s en parole, 1.7s en écoute, opacité 0.55↔1 + brightness 1.4). Vérifié Playwright : clic → mode Écoute → opacity 0.98, animationName boltFlash, halo visible sur capture.
- **10/06/2026 — Lecture vocale des mails Outlook** : App.js readMailAloud() — GET /api/microsoft/mail?top=5 (Graph par utilisateur) avec repli sur l'ancien /api/outlook/emails ; énonce à voix haute le nombre de non-lus + les 3 derniers (expéditeur, sujet, aperçu si non lu) et affiche le détail dans une fenêtre de tâche « OUTLOOK — LECTURE DES MAILS » ; si aucun connecteur : message vocal invitant à connecter Microsoft. Déclencheurs : regex \b(lis|lis-moi|lire|lecture)\b…(mails|e-mails|courriels) dans processCommand + module outlook_read dans INTENT_PROMPT (vérifié curl : « lis-moi mes derniers mails outlook à voix haute » → open_module outlook_read) + executeIntent. INCIDENT (récurrence x3) : première insertion du bloc readMailAloud perdue silencieusement (ReferenceError) → ré-insérée, vérifié Playwright : « lis mes mails » → fenêtre + parcours complet OK.
- **10/06/2026 — Effet holographique autour du gardien restauré** : les panneaux holographiques cyan autour de Zeus font partie de zeus-boot.jpg (perdus lors du détourage). App.js : img → /holo/zeus-boot.jpg avec classe .holo-full ; CSS : mêmes masques que la présentation MAIS corrigés — le -webkit-mask-image mono-gradient écrasait le masque double (alias Chrome) → double gradient (fondu haut 0-7%, bas 62-96%, côtés 0-12%/88-100%) + mask-composite intersect / -webkit source-in, appliqué aussi à .boot-zeus img ; vignette radiale .guardian-rig::before ; sway désactivé pour l'image rectangulaire (pulse conservé en parole/écoute) ; bolt-glow repositionné (left 13%, width 30%). zeus-gardien.png (détouré) conservé sur disque si besoin futur. Vérifié capture : Zeus + panneaux holo fondus dans le décor.

## Session 10/08/2026 — Netteté du noyau HUD + position gardien (fork)
- Noyau HUD identique à la page de présentation : suppression de `mix-blend-mode: screen` + `clip-path: circle(40%)` sur `.reactor-wrap .reactor-canvas` (rendait le centre délavé/blanchâtre) ; `.reactor-wrap .core-pulse { display:none }` retiré (croissants cyan restaurés comme sur la présentation).
- Masque du `.core-pulse` corrigé : `radial-gradient(circle closest-side, transparent 76%, #000 82%, #000 97%, transparent 100%)` — l'ancien masque (farthest-corner 55-81%) débordait de la boîte et créait un gros croissant coupé net (bord droit plat, très laid). Opacité du mode écoute 0.8 → 0.6.
- Titre ΣIRIUS déplacé SOUS le médaillon (comme la présentation) : override fin d'App.css `.reactor-wrap .reactor-text { inset:auto; top:calc(100% + 2px); left:50%; translateX(-50%) }` + titre réduit clamp(48px,9vh,96px).
- Lueur floue derrière le noyau supprimée : `.center-stage::before { content:none !important }` (toutes variantes data-phase).
- Gardien déplacé un peu à droite et remonté : `.guardian-rig { left: calc(8% + 110px); bottom: 4vh }` (l'ancienne règle `.antique-guardian.holo-full { left/top }` était sans effet — position:static dans le rig — supprimée).
- PIÈGE ÉVITÉ/RENCONTRÉ : append heredoc sur App.css a dupliqué un fragment en fin de fichier (erreur PostCSS, page rouge) — corrigé. NE PAS utiliser cat >> sur App.css, préférer search_replace.
- Vérifié par captures zoom : noyau cyan éclatant et net, croissants le long du bord, présentation inchangée, HUD complet OK.

## Session 10/08/2026 (suite) — Pilules HEURE / NOYAU / MÉTÉO
- Panneaux HEURE (bottom-left), NOYAU/mode (bottom-right) et MÉTÉO (float-card fc-tr) restylés en barre pilule compacte (même ratio que la barre SIRIUS–ACTIVITÉ) : hauteur 46px, border-radius 999px, fond noir dégradé, bordure or 0.55, inscriptions or, contenu sur UNE ligne (flex row).
- Overrides ajoutés en FIN d'App.css (après le thème noir&or) : titres sans bordure basse, heure 17px + date 9px inline, .noyau-state en badge doré, .noyau-user masqué (info déjà en haut à droite), météo icône 18px + temp 17px + ciel inline.
- Repositionnement NOYAU : déplacé en haut à droite SOUS la pilule MÉTÉO (top: calc(8% + 118px); right: 4%) car en bas à droite il était caché par la fenêtre d'activité (z-index 95) puis chevauchait le bouton ENVOYER. MÉTÉO est ~y122 réel (top 8% de .floating-panels dans center-stage) — NE PAS remettre NOYAU à top calc(8%+60px), ça superpose les deux pilules.
- Vérifié par captures : 3 pilules noires/or alignées au style de la photo utilisateur, activité intacte.

## Session 10/08/2026 (suite 2) — Pilule CPU/RAM
- .hud-panel.stats-mini ajouté au groupe pilule (noir/or, 46px, radius 999px) : flex-direction row, CPU et RAM côte à côte, repositionné à left:30px bottom:88px (juste au-dessus de la pilule HEURE). Vérifié par capture.

## Session 10/08/2026 (suite 3) — RÉVEIL MATINAL
- Nouveau module ReveilPanel.jsx (lazyModules + entrée menu SYSTÈME « RÉVEIL MATINAL », icône AlarmClock) : heure (input time), toggle activer/désactiver, statut « Réveil armé — aujourd'hui/demain à X h ». Config localStorage `sirius_reveil` {on, time}; anti-redéclenchement quotidien `sirius_reveil_last` (date), effacé à chaque modification.
- Planificateur (App.js, effet après runBriefing) : setInterval 20 s → à HH:MM exact, salut vocal « Réveil ! Il est X heures... », puis runBriefing(true) (inclut météo Atlas, agenda, rappels Thémis, marchés), attente fin de parole (poll speakingRef, timeout 4 min), puis readMailAloud() (mails Outlook à voix haute).
- Commandes vocales (processCommand, bloc avant le briefing ~l.3273) : « réveille-moi à 7h30 » / « règle le réveil à X » (réglage+activation), « active le réveil », « désactive le réveil », « ouvre le réveil ». Intent marks : réveil · réglage/on/off/ouverture.
- Testé E2E par playwright : déclenchement réel à l'heure programmée (briefing lancé puis tâche OUTLOOK — LECTURE DES MAILS ; « Compte Microsoft non connecté » normal sur compte test), commandes texte OK (config vérifiée en localStorage, intent loggé). NB : un 1er test « désactive » avait échoué car envoyé 3 s après la commande précédente (artefact), isolé il passe.
- CSS : bloc .reveil-* en fin d'App.css (noir/or, badge statut pilule).

## Session 10/08/2026 (suite 4) — SPOTIFY dans le HUD (lecteur intégré + display)
- Backend : POST /api/spotify/search (tokens user, refresh auto comme now-playing) → 8 titres {id, title, artist, album, image, image_big, duration_ms}. Testé curl (401 sans token).
- SpotifyPanel.jsx (lazyModules, fenêtre prime-screen noir/or) : bouton CONNECTER si non connecté ; recherche ; clic sur un titre → iframe embed https://open.spotify.com/embed/track/{id} (lecture complète si connecté à Spotify dans le navigateur, sinon extrait 30 s) + pochette envoyée dans SIRIUS DISPLAY (showOnDisplay type image) ; bouton écran par titre → /spotify/play (appareil actif, Premium) ; bouton EN ÉCOUTE → fetchNowPlaying.
- fetchNowPlaying enrichi : pochette du morceau en cours affichée dans SIRIUS DISPLAY (type image, legende SPOTIFY — titre · artiste).
- Entrée menu MÉDIAS « Spotify — lecteur intégré » ouvre la fenêtre (avant : connect/now-playing direct).
- Commandes : « ouvre/lance spotify » (fin de phrase uniquement → ne capte PAS « lance X sur spotify » qui reste intent musique, vérifié), « ferme spotify ». Intents spotify · lecteur / fermeture vérifiés via /api/prime/log.
- PIÈGE TESTS : l'assistant INSTALLATION s'auto-ouvre à chaque session headless fraîche et vole les clics/focus (échecs de clics playwright). Pour piloter la barre de commande, utiliser evaluate + requestSubmit du form [data-testid=sirius-cmd-form].

## Session 10/08/2026 (suite 5) — GMAIL (onglet + lecture HUD/voix)
- Backend (google_calendar.py) : scope OAuth étendu à gmail.readonly (reconnexion Google requise pour l'accorder). Nouveaux endpoints : GET /api/gmail/status {connected, email, gmail_scope} ; GET /api/gmail/messages?top=5 {non_lus (label UNREAD), mails[{de, sujet, apercu, lu, date}]} via Gmail API metadata. Erreurs guidées : 401 session expirée/non connecté, 403 scope manquant (→ reconnecter) vs API Gmail non activée (→ console Google Cloud).
- Frontend : readGmailAloud(openOnly) — fenêtre de tâche GMAIL (liste 5 mails + NON LUS) + lecture vocale des 3 derniers (ou annonce seule si openOnly). Commandes AVANT les handlers Outlook : « lis mes mails gmail » (lecture), « montre/affiche ma boîte gmail » (fenêtre), « ouvre gmail » (nouvel onglet mail.google.com). « lis mes mails » (sans gmail) reste Outlook — vérifié (intents gmail·onglet/lecture, outlook·lecture vocale via prime/log).
- Testé : curl status/messages (401 correct sans connexion), les 3 commandes E2E. Lecture réelle non testable sans compte Google connecté avec API Gmail activée (action utilisateur : activer Gmail API dans sa console + reconnecter Google via l'Agenda).
